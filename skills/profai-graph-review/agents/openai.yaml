# Codex skill presentation metadata.
# Schema: `interface` / `policy` / `dependencies` only — this file carries no
# instructions. The protocol lives in SKILL.md, which Codex loads by convention.
interface:
  display_name: "PROF AI Graph Review"
  short_description: "Two-engine defect hunt for agentic systems"
  default_prompt: "Run a graph review on the slice I just finished and hand me a decision list."
